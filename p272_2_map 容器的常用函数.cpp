/*

m.insert(pair)		先将pair这个数据对插入map容器m中，然后map容器将根据这个数据对的键对其进行排序，也就是说，map容器始终是排序好的
m.size()			返回容器m中元素的个数
m.count(KEY)		判断KEY这个键是否在容器m中出现，如果出现则返回1；否则返回0
m.find(KEY)			返回容器m中指向键为KEY的数据对的迭代器
m.earse(pos)		删除容器m中pos迭代器所指向的元素
m.empty()			判断容器m是否为空
m.clear()			删除容器m中的所有元素

*/