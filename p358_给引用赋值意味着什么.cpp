/*
给引用赋值意味着什么？
	引用就是另外一个对象的别名，给引用赋值，就是将引用指向这个对象。
此后对引用的任何操作，实际上就是对这个对象的操作。
	记住：引用就是目标对象的指示物。所以当定义一个引用时，必须给引用赋值，
将它指向某个对象。并且引用和它所指向的对象之间的这种指向关系一旦确定就
不能更改，也就是说，不能修改引用的初始指向，将其指向另外一个对象，引用和它所指向
的对象之间的关系是从一而终的。
*/